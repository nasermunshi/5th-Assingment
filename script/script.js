const button = document.getElementsByClassName("button-click")
console.log(button, );
for (let btn = 0; index < array.length; index++) {
 const element = btn[index]; 
 console.log(element);                 
}
